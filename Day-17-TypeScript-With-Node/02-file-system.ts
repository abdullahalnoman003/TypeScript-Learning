// =============================================================================
// DAY 17 — FILE 02: TypeScript with Node.js — File System & Path
// =============================================================================
//
// TOPICS TO COVER:
//   - Using fs/promises with async/await + TypeScript types
//   - Typed path operations with the 'path' module
//   - Reading/writing JSON files with typed content
//   - Watching for file changes with typed events
//   - Environment variables with type-safe config objects
//   - Child processes with typed output
//
// TODO: Fill in this file when you reach Day 17
//
// EXAMPLE STRUCTURE:
//
// import { readFile, writeFile } from "fs/promises";
// import path from "path";
//
// interface AppConfig { port: number; host: string; debug: boolean; }
//
// async function readConfig(filepath: string): Promise<AppConfig> {
//     const raw = await readFile(filepath, "utf-8");
//     return JSON.parse(raw) as AppConfig;
// }
//
// async function writeConfig(filepath: string, config: AppConfig): Promise<void> {
//     await writeFile(filepath, JSON.stringify(config, null, 2), "utf-8");
// }
//
// const configPath = path.join(__dirname, "config.json");
// readConfig(configPath).then(c => console.log(`Port: ${c.port}`));
//
// =============================================================================

// Your code here ↓

export {};
