// =============================================================================
// DAY 16 — FILE 02: Async/Await with TypeScript
// =============================================================================
//
// TOPICS TO COVER:
//   - async function return type is always Promise<T>
//   - await narrows the Promise<T> to T
//   - try/catch for async error handling
//   - Parallel execution with Promise.all + async/await
//   - Async arrow functions
//   - Async class methods
//   - Top-level await (ESNext modules)
//
// TODO: Fill in this file when you reach Day 16
//
// EXAMPLE STRUCTURE:
//
// interface Post { id: number; title: string; }
//
// async function fetchPost(id: number): Promise<Post> {
//     const response = await fetch(`https://jsonplaceholder.typicode.com/posts/${id}`);
//     if (!response.ok) throw new Error(`HTTP ${response.status}`);
//     return response.json() as Promise<Post>;
// }
//
// async function main(): Promise<void> {
//     try {
//         const post = await fetchPost(1);
//         console.log(post.title);
//     } catch (err) {
//         if (err instanceof Error) console.error(err.message);
//     }
// }
//
// main();
//
// =============================================================================

// Your code here ↓

export {};
